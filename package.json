{
  "name": "caricash",
  "version": "0.0.1",
  "private": true,
  "description": "Caricash Barbados - Fintech Platform",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "migrate": "node packages/db/dist/migrate.js",
    "dev": "docker compose up -d && pnpm -r --parallel dev",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "argon2"
    ]
  }
}
